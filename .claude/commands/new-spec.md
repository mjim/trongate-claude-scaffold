Create a new spec file in `_reference/specs/` for the feature described below.

Use the template at `_reference/specs/SPEC_TEMPLATE.md` as the structure.

Before writing anything, ask me clarifying questions to properly fill out:
- Problem and goal
- What is in scope and out of scope
- Functional requirements
- Any data or schema changes needed
- Edge cases worth noting

Once I've answered, generate the spec and save it to `_reference/specs/[feature-slug].md`.

Feature: $ARGUMENTS
